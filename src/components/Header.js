import React from 'react';


const header = (props) => {
    return(
       <div className="Header">
          <h1>Digitales Rezept</h1>
          <div>
            <button>Logout</button>
          </div>
       </div>

    )
};

export default header;
